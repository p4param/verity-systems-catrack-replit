# ⚠️ This location has moved

This folder is retained temporarily as a redirect notice per the docs/ repository migration (see `docs/project-governance/MIGRATION-LOG.md`). It will be removed in a future cleanup pass — do not add new content here.

Its former contents now live at:

- **Bootstrap documents** (`APP-001.md`, `Bootstrap-v3.md`, `VAP-Bootstrap-v2.0.md`) → [`docs/bootstrap/`](../bootstrap/)
- **Architecture** (`CAP-v1.0-Architecture.md`) → [`docs/architecture/`](../architecture/)

For the full documentation map, start at [`docs/README.md`](../README.md).
