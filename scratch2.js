const { NavigationService } = require('./src/modules/platform/navigation/services/navigation-service');
const { PrismaClient } = require('./src/generated/client');

async function main() {
  // We have to transpile or require TS. We can use ts-node or just query it manually
}
main();
